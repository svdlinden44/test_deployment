[build]
builder = "railpack"

[deploy]
startCommand = "python manage.py repair_migration_history --execute && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn config.wsgi --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
